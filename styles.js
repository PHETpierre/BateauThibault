import { StyleSheet } from 'react-native'

const globalStyle = StyleSheet.create({
  red: {
    color: 'red'
  },
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  backgroundImage: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
  },
})

export { globalStyle }
